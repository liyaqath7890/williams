# Database Schema

Core tables: users, sessions, sos_incidents, alerts, shelters, resources, missing_persons, uploads, notifications, chat_rooms, chat_messages.

Additive production fields include resource assignments/metadata, missing-person family contacts/timeline/last-seen history/notes, upload Cloudinary metadata, user notification preferences in `users.metadata`, and notification read state.
